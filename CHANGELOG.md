# 更新日志

用户可见变更在前，内部/工具链变更在后（不影响游戏内行为）。开发向明细见 `git log`。

## 0.5.9 — 2026-09-11

覆盖 **0.5.7 → 0.5.9**（0.5.8 为内部版本，未公开发布，其变更并入本版）。

### 配装

- 因子组合改为**提示制**：非法组合只灰显 + 红框提示，不再阻止选择、保存与实装；已选内容也不会被清空。
- 组合规则（主副双向判定）：唯一持有因子与「技能自身」因子不参与组合；锁定因子只能配它的池内因子或固定副；其余因子可自由组合。
- 池版因子的可选范围补入专属因子行。
- 3 条进阶专属因子改用技能基础名显示（不带「＋」后缀）。

### 数据字段与校验

- `sigils.json` 字段名与生成表列名对齐：原 `sec` → `skill2`、`special` → `onlyone`，并新增 `mix` 字段。
- 角色限制改由 `sigils.json` 专属行的 `character` 字段驱动；该字段缺失或条目数不符时**启动即失败**（fail-closed），不再静默降级。

### 配装编辑器

- 修复：隐藏工具后按 F1 唤不出工具（隐藏时把焦点交还召唤前的前台窗口）。
- 修复：退出工具后鼠标箭头残留在屏幕上。
- 修复：热键卸载时序保护。
- 界面拆分重构（i18n / 模型 / 槽位编辑器 / 专属页），槽位行 memo 化减少重绘制。

### 构建与部署

- 构建前强制校验数据一致性（`sigils.json` ↔ `sigils.xlsx`、专属表 ↔ C++ 表段），不一致直接失败。
- 新增单文件生成器：一条命令产出 `sigils.xlsx` 与 `sigils.json`，无中间产物。
- 修复部署后工具未自动重开；部署后校验工具存活，失败重试一次。
- 修复门禁在缺少数据源时静默跳过的问题（改为直接失败）。
- 构建/部署脚本必须用 **PowerShell 7**（Windows PowerShell 5.1 会让中文提示乱码）。

### 内部 / 工具链（不影响游戏内行为）

- 代码清理与简化：删死代码、死 include、死枚举值、死函数，去重复实现，统一日志契约与跨语言常量。
- 原生字符串格式化统一为 `std::format`（原生 DLL 668 KB → 850 KB，仅体积变化）。
- 生成管线整理：数据目录并入仓库、生成器幂等写回 C++ 表段、xlsx 样式与字段文档同步、`gem.xlsx` 更名 `sigils.xlsx`。
- 新增 `.gitattributes`（统一 LF；`.bat` / `.sln` / `.vcxproj` 保持 CRLF）。
- 维护手册精简（323 → 271 行）、补上缺号小节、清理失效引用与重复段落。
