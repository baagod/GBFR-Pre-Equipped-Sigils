# 更新日志

只列**用户可见**变更。开发向明细见 `git log`；跨语言常量与雷区见 `docs/MAINTENANCE.md`。

## 0.5.9 — 2026-09-11

> 0.5.8 为未公开发布的内部版本，其变更已并入本版；因此从 0.5.7 升级即为本版全部内容。

**配装**

- 因子组合改为**提示制**：非法组合只灰显 + 红框提示，不再阻止选择、保存与实装；已选内容也不会被清空。
- 组合规则：唯一持有因子与「技能自身」因子不参与组合；锁定因子只能配它的池内因子或固定副；其余因子可自由组合。
- `sigils.json` 字段名与生成表列名对齐；池版因子的可选范围补入专属行。

**配装编辑器**

- 修复：隐藏工具后按 F1 唤不出工具。
- 修复：退出工具后鼠标箭头残留在屏幕上。
- 界面拆分重构（i18n / 模型 / 槽位编辑器 / 专属页），减少重绘制。

**构建与部署**

- 新增单文件生成器：一条命令产出 `sigils.xlsx` 与 `sigils.json`，无中间产物。
- 构建前校验数据一致性，不一致直接失败（避免发出与数据源不符的包）。
- 修复部署后工具未自动重开。
- 修复门禁在缺少数据源时静默跳过的问题（改为直接失败）。

**文档**

- 构建/部署说明修正为**必须使用 PowerShell 7**（Windows PowerShell 5.1 会让中文提示乱码）；两份 README 同步。
- 维护手册精简并清理失效引用。
